# Docker Environment Variables for PaiNaiDee 3D Map
# ตัวแปรสภาพแวดล้อม Docker สำหรับ PaiNaiDee 3D Map

# Container Configuration / การตั้งค่าคอนเทนเนอร์
CONTAINER_NAME=painaidee-3d-map
IMAGE_NAME=painaidee-3d-map
IMAGE_TAG=latest

# Port Configuration / การตั้งค่าพอร์ต
HOST_PORT=80
CONTAINER_PORT=80
DEV_PORT=3000

# Nginx Configuration / การตั้งค่า Nginx
NGINX_USER=painaidee
NGINX_WORKER_PROCESSES=auto
NGINX_WORKER_CONNECTIONS=1024

# Security Settings / การตั้งค่าความปลอดภัย
NGINX_SERVER_TOKENS=off
NGINX_CLIENT_MAX_BODY_SIZE=10m

# Cache Settings / การตั้งค่าแคช
NGINX_GZIP=on
NGINX_GZIP_COMP_LEVEL=6
STATIC_CACHE_DURATION=31536000

# Logging / การบันทึกล็อก
LOG_LEVEL=warn
ACCESS_LOG=/dev/stdout
ERROR_LOG=/dev/stderr

# Health Check / การตรวจสอบสุขภาพ
HEALTHCHECK_INTERVAL=30s
HEALTHCHECK_TIMEOUT=3s
HEALTHCHECK_RETRIES=3

# Resource Limits / ขำหนดทรัพยากร
MEMORY_LIMIT=256m
CPU_LIMIT=0.5

# Development Settings / การตั้งค่าการพัฒนา
DEVELOPMENT_MODE=false
LIVE_RELOAD=false

# Multi-stage Build Targets / เป้าหมายการสร้างหลายขั้นตอน
BUILD_TARGET=production  # development, production

# Instructions / คำแนะนำ:
# Copy this file to docker.env and modify values as needed
# คัดลอกไฟล์นี้เป็น docker.env และแก้ไขค่าตามต้องการ
# 
# Usage with docker-compose:
# docker-compose --env-file docker.env up
#
# การใช้งานกับ docker-compose:
# docker-compose --env-file docker.env up